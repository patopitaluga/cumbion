/**
 * Reserved words.
 *
 * @return {array}
 */
const reservedWords = [
  // 'de', // sets that number as the index of an array <- this one might be the number 2 in some context
  'baja', // --
  'entre', // divide (Math)
  'es', // =
  'escucha', // =
  'lxs', // is the let in an array definition
  'mandale', // return
  'menos', // - sign (Math)
  'mientras', // while
  'misterioso', 'misteriosa', // undefined
  'ortiba', // false
  'piola', // true
  'pone', // to set an already declared variable
  'por', // * (Math)
  'para', // Pará! to close a loop without letting an empty line (if it's actually "para" might be ignores anyway)
  'si', // if
  'sube', 'crece', // ++
  'quieren', 'tienen', // is the [ in an array definition
  'y',
  'wuki',
];

export { reservedWords };
